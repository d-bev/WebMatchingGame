<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>Matching Game</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="">
        <style>
            .error {
                background-color: red;
            }
            form, fieldset, p, p>label{
                background-color: lightsteelblue;
                color: black;
            }
            legend {
                background-color: lightsteelblue;
            }
            p{
                display: grid;
                grid-template-columns: 2, auto;
            }
            p>input {
                background-color: lightsteelblue;
            }
            input[type="submit"]{
                background-color: lightsteelblue;
            }
            a:active{
                color:black;
            }
            a:checked{
                color: darkslategrey;
            }
        </style>
    </head>
    <body>
        <h1>Bejeweled Is Better</h1>
        <form action="/login" method="POST">
            <fieldset>
                <legend>Log-In Form</legend>
                <!--    we don't need to validate the user's display name here-->
                <p>
                    <label for="username">Username: </label>
                    <input type="text" name="username" id="username" placeholder="Enter a username" required>
                    <span id="username" class="error">
                        <% if(usernameError.length > 0) { %>
                            Error: <%= usernameError %>
                        <% } %>
                    </span>
                </p>
                <p>
                    <label for="password">Password: </label>
                    <input type="password" name="password" id="password" placeholder="Enter a password" required>
                    <span id="password" class="error">
                        <% if(passwordError.length > 0) { %>
                            Error: <%= passwordError %>
                        <% } %>
                    </span>
                </p>
                <br>
                <input type="submit" value="Log In" readonly>
                <br><br><hr>
                <p>
                    Don't have an account?
                    <a href="/register">Register Here</a>
                </p>
                <hr>
                <p>
                    Play as a Guest User?
                    <a href="../static/index.html">Static File For Presentation</a>
                </p>
            </fieldset>
        </form>
        <br>
        <div class="nav">
            <button><a href="/register">Registration Page</a></button>
            <button><a href="/show">Show Submissions</a></button>
            <button><a href="/logout">Log Out</a></button>
        </div>
    </body>
</html>